#include <stdio.h>
/**
 * main -  writes name
 * @argc: arguement count
 * @argv: array of pointer to argument string
 * Return: 0
 **/
int main(int argc, char *argv[])
{
argc = argc;
printf("%s\n", *argv);
return (0);
}
